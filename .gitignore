# EDK2 source tree — fetched by setup-edk2
edk2/

# QEMU test ESP — FAT image, not source
esp/

# Per-device TPM provisioning state — never commit (contains stretched password hash)
tpmstate/

# Nix/direnv caches
.direnv/
result

# EDK2 build artefacts (if build-shell is run from repo root)
Build/

# clangd index (generated by bear during build)
compile_commands.json

# Old design artefact
pepper.bin
